const lab = exports.lab = require('lab').script()
const { before, beforeEach, describe, it } = lab
const { expect } = require('code')

describe('UserController', () => {
    describe('GET /authorization/api/users/{userId}/permissions', () => {

    })

    describe('GET /authorization/api/users/{userId}/roles', () => {

    })

    describe('POST /authorization/api/users/{userId}/roles', () => {

    })

    describe('DELETE /authorization/api/users/{userId}/roles/{roleId}', () => {

    })
})
