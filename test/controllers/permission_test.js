const lab = exports.lab = require('lab').script()
const { before, beforeEach, describe, it } = lab
const { expect } = require('code')

describe('PermissionController', () => {
    describe('GET /authorization/api/permissions', () => {

    })

    describe('POST /authorization/api/permissions', () => {

    })

    describe('PUT /authorization/api/permissions/{permissionName}', () => {

    })

    describe('DELETE /authorization/api/permissions/{permissionName}', () => {

    })
})
