const lab = exports.lab = require('lab').script()
const { before, beforeEach, describe, it } = lab
const { expect } = require('code')

describe('RoleController', () => {
    describe('GET /authorization/api/roles', () => {

    })

    describe('GET /authorization/api/roles/{roleId}', () => {

    })

    describe('POST /authorization/api/roles', () => {

    })

    describe('PUT /authorization/api/roles/{roleId}', () => {

    })

    describe('DELETE /authorization/api/roles/{roleId}', () => {

    })
})
